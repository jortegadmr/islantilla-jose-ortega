{% extends 'base.html.twig' %}

{% block title %}Editar Habitacion{% endblock %}

{% block body %}
<div class="container">
        <!-- Título de la página -->
        <h1 class="titulo text-center mt-4">Editar Habitacion</h1>
        
        <!-- Formulario de Edición -->
        <div class="formulario mt-3 text-center">
            {{ include('habitacion/_form.html.twig', {'button_label': 'Actualizar'}) }}
        </div>

        <!-- Botones de Navegación y Eliminación -->
        <div class="d-flex justify-content-between mt-4">
            
            <div class="text-center">
                <a href="{{ path('app_habitacion_index') }}" class="btn boton-mostrar">Volver a la lista</a>
            </div>

        <div class="text-center">

            {{ include('habitacion/_delete_form.html.twig') }}
        </div>
    </div>
</div>    

{% endblock %}
