<form method="post" action="{{ path('app_actividades_delete', {'id': actividade.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ actividade.id) }}">
    <button class="btn">Delete</button>
</form>
