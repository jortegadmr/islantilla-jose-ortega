{% extends 'base.html.twig' %}

{% block title %}Editar Actividades{% endblock %}

{% block body %}
    <h1>Editar Actividades</h1>

    {{ include('actividades/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_actividades_index') }}">Volver a la lista</a>

    {{ include('actividades/_delete_form.html.twig') }}
{% endblock %}
